[dclient-dserver_dserver.server]
BROKER_HOST = localhost
BROKER_PORT = 5672
USER = dserver
PASSWORD = J9ma4iFT
VHOST = dclient-dserver
CLIENT_EXCHANGE = dclient.client
SERVER_EXCHANGE = dserver.server
SERVER_QUEUE = dserver.server
CLIENT_QUEUE_PREFIX = dclient.client

[dbundlers-dserver_dserver.client]
BROKER_HOST = localhost
BROKER_PORT = 5672
USER = dserver
PASSWORD = J9ma4iFT
VHOST = dbundlers-dserver
CLIENT_EXCHANGE = dserver.client
SERVER_EXCHANGE = dbundlers.server
SERVER_QUEUE = dbundlers.server
CLIENT_QUEUE_PREFIX = dserver.client

[dinstallers-dserver_dserver.client]
BROKER_HOST = localhost
BROKER_PORT = 5672
USER = dserver
PASSWORD = J9ma4iFT
VHOST = dinstallers-dserver
CLIENT_EXCHANGE = dserver.client
SERVER_EXCHANGE = dinstallers.server
SERVER_QUEUE = dinstallers.server
CLIENT_QUEUE_PREFIX = dserver.client

